<main>
  <div class="container py-1">
    <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Bem vindo ao Blog</h1>
        <p class="col-md-8 fs-4">Using a series of utilities, you can create this jumbotron, just like the one in previous versions of Bootstrap. Check out the examples below for how you can remix and restyle it to your liking.</p>
        <button class="btn btn-primary btn-lg" type="button">Cadastre-se</button>
      </div>
    </div>

    <hr>
    <h2>Recentes do Blog</h2>
 
    {{#each postagens}}
      <div class="card card-body mt-2">
        
          
              <h2>{{titulo}}</h2>
              <h3>{{subtitulo}}</h3>
              <blockquote>"{{descricao}}"</blockquote>

              <p><small>Slug: <em>{{slug}}</em></small></p>
              <p><small>Categoria: {{categoria.nome}}</small></p>
              <p><small><em>{{date}}</em></small></p>
              <a href="/postagem/{{slug}}"><button class="btn btn-outline-primary" type="button">Saiba mais</button></a>
        
        
      </div>
    {{else}}
          <div class="row align-items-md-stretch">
            <div class="col-md-6">
              <div class="h-100 p-5 text-white bg-dark rounded-3">
                <h2>Nenhuma postagem encontarada</h2>
                <p>Tente cadastrar novos posts.</p>
                <button class="btn btn-outline-light" type="button">Criar posts</button>
              </div>
            </div>
          </div>

    {{/each}}
    
    </div>
    <footer class="pt-3 mt-4 text-muted border-top">
      &copy; 2021
    </footer>
    
  </div>
</main>
